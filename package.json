{
	"name": "skillmd_collection",
	"private": true,
	"version": "0.0.0",
	"description": "Monorepo of Claude Code agent skills and CLI tools",
	"packageManager": "pnpm@9.15.0",
	"engines": {
		"node": ">=20"
	},
	"scripts": {
		"build": "pnpm -r run build",
		"typecheck": "pnpm -r run typecheck",
		"test": "pnpm -r run test",
		"dev:fastbrowser": "pnpm --filter fastbrowser_cli run dev:cli",
		"clean": "pnpm -r exec rm -rf dist node_modules && rm -rf node_modules"
	}
}
